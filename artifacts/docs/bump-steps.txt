STEP 1
------
git checkout master && g pull && git merge continuous --no-ff -m "chore(merge): continuous" && npm run bump

STEP 2
------
git commit -am "chore(bump): v0.0.6" && g push

STEP 3
------
git checkout continuous && git pull && git merge master -m "chore(merge): master" && git push